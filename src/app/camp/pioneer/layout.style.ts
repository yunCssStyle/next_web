import COLOR from '@/constants/COLOR';
import styled from '@emotion/styled';

export const PioneerLayoutStyle = styled.div`
  width: 100%;
  display: flex;
  flex-direction: column;
  .refresh__box {
    margin: 40px 0 16px;
    height: 48px;
    display: flex;
    justify-content: flex-end;
  }
`;
